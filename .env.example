# https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true

# Email credentials
EMAIL_USER=info@doddapanenigroup.net
EMAIL_PASS="Lakshmi@123456#"

# NextAuth (required for login)
# Generate with: openssl rand -base64 32
AUTH_SECRET=X9n0VDIxtz37SbPIfLrAx7prCS2YDxEUzIz7YJDu/eM=

# NextAuth URL (required in production if behind a proxy)
# For localhost: http://localhost:3000
# For production: https://yourdomain.com
NEXTAUTH_URL=https://doddapanenigroup.net
# Reset-and-seed script: Super Admin user (set in .env.local, never commit real values)
SUPER_ADMIN_EMAIL=lk8772000@gmail.com
SUPER_ADMIN_PASSWORD=Lokesh@0317
SUPER_ADMIN_NAME=lokesh

# PostgreSQL (required) — use this same URL in Hostinger env vars
DATABASE_URL="postgresql://doddapanenigroup:root123@localhost:5432/doddapaneni_group?schema=public"

#npx prisma db push
#npm run db:seed    # or npm run db:reset for a full wipe + super-admin only