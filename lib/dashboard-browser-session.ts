/** sessionStorage key: cleared when all tabs for this origin are closed. */
export const DASHBOARD_BROWSER_SESSION_KEY = 'dg-dashboard-browser-session';

/** Cross-tab sync so a new tab inherits the marker while any sibling tab is still open. */
export const DASHBOARD_BROWSER_SESSION_BC = 'dg-dashboard-browser-session';
